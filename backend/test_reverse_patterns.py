#!/usr/bin/env python3
"""
Тест для проверки паттернов с обратным порядком слов
"""

import sys
import os
sys.path.append(os.path.join(os.path.dirname(__file__), 'src'))

from src.ai_conversation.location_service import extract_location_simple

def test_reverse_patterns():
    """Тестируем паттерны с обратным порядком слов"""
    
    test_cases = [
        # Обратный порядок слов
        ("10 компаний в области Улытау", "Улытауская область"),
        ("Найди компании в области Алматы", "Алматинская область"),
        ("Покажи 5 компаний в области Атырау", "Атырауская область"),
        ("Компании в области Караганда", "Карагандинская область"),
        ("Найди 15 компаний в области Костанай", "Костанайская область"),
        ("Покажи компании в области Мангистау", "Мангистауская область"),
        ("Найди 20 компаний в области Павлодар", "Павлодарская область"),
        ("Компании в области Жамбыл", "Жамбылская область"),
        ("Найди компании в области Восточный Казахстан", "Восточно-Казахстанская область"),
        ("Покажи 10 компаний в области Западный Казахстан", "Западно-Казахстанская область"),
        ("Компании в области Акмола", "Акмолинская область"),
        ("Найди 25 компаний в области Южный Казахстан", "Южно-Казахстанская область"),
        
        # Обычный порядок слов (для сравнения)
        ("10 компаний в Улытауской области", "Улытауская область"),
        ("Найди компании в Алматинской области", "Алматинская область"),
        ("Покажи 5 компаний в Атырауской области", "Атырауская область"),
    ]
    
    print("🧪 Тестируем паттерны с обратным порядком слов...")
    print("=" * 70)
    
    success_count = 0
    total_count = len(test_cases)
    
    for i, (query, expected) in enumerate(test_cases, 1):
        print(f"\n📝 Тест {i}: '{query}'")
        
        location = extract_location_simple(query)
        print(f"📍 Извлеченная локация: {location}")
        print(f"🎯 Ожидаемая локация: {expected}")
        
        if location == expected:
            print("✅ УСПЕХ: Локация извлечена правильно")
            success_count += 1
        else:
            print("❌ ОШИБКА: Локация извлечена неправильно")
    
    print("\n" + "=" * 70)
    print(f"📊 Результаты: {success_count}/{total_count} тестов прошли успешно")
    
    if success_count == total_count:
        print("🎉 ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО!")
    else:
        print(f"⚠️ {total_count - success_count} тестов не прошли")

if __name__ == "__main__":
    test_reverse_patterns() 